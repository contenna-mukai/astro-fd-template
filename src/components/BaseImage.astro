---
interface Source {
	srcset: string;
	type?: string;
	media?: string;
}

interface Props {
	src: string;
	alt?: string;
	class?: string;
	sources?: Source[]; // picture 用
}

const { src, alt = '', class: className, sources = [] } = Astro.props;
---

{
	sources.length > 0 ? (
		<picture>
			{sources.map((s) => (
				<source srcset={s.srcset} {...(s.type ? { type: s.type } : {})} {...(s.media ? { media: s.media } : {})} />
			))}
			<img src={src} alt={alt} {...(className ? { class: className } : {})} />
		</picture>
	) : (
		<img src={src} alt={alt} {...(className ? { class: className } : {})} />
	)
}
